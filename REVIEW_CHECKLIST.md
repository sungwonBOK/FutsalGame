# 교차 리뷰 체크리스트

현재 작업과 diff만 검토한다. 모든 지적에는 파일·위치, 영향, 구체적인 수정안 또는 검증 방법을 포함한다.

## 1. 범위와 동작
- 모든 인수 조건을 만족하는가?
- 작업 밖 기능, 리팩터링, 삭제가 포함됐는가?
- PRD·설계·ADR 또는 현재 로컬 프로토타입 범위와 충돌하는가?

## 2. Unity 정확성
- Runtime과 Editor 전용 의존성이 분리됐는가?
- Unity 생명주기, 직렬화, 참조, null 상태를 안전하게 처리하는가?
- 플레이어는 `CharacterController`, 공은 `Rigidbody` 경계를 유지하는가?
- 입력 장치별 게임 로직 대신 Input Action을 통해 처리하는가?
- 프레임 의존 동작이 올바른 시간·물리 업데이트 경로를 사용하는가?

## 3. 에셋과 저장소 안전성
- `.unity`, `.prefab`, `.asset` YAML을 직접 수정했는가?
- Scene, Prefab, Input, Package, ProjectSettings 변경이 점유됐고 필요한가?
- `.meta`가 보존되고 에셋과 함께 변경됐는가?
- 자동 생성·개인·무관 파일이 diff에 없는가?

## 4. 설계 품질
- 동작을 만족하는 가장 작은 일관된 구현인가?
- 기존 패턴을 따르며 성급한 프레임워크·추상화를 추가하지 않았는가?
- 다음 시스템이 사용할 공개 인터페이스와 책임이 명확한가?
- 오류와 미지원 상태를 조용히 무시하지 않는가?

## 5. 검증
- 작성자가 명시한 테스트와 컴파일 검사를 실제 실행했는가?
- Unity Console·Play Mode 확인과 추정이 명확히 구분됐는가?
- 다른 개발자가 재현할 수 있는 수동·회귀 절차가 있는가?

## 심각도
- **Blocker:** 요구사항, 빌드, 에셋·데이터, 핵심 동작을 깨므로 병합 불가.
- **Important:** 병합 전 수정해야 할 가능성 높은 결함 또는 유지보수 위험.
- **Suggestion:** 작업 완료를 막지 않는 선택적 개선.
