# 2인 개발 협업 절차

## 1. 소유권
- 사람 담당자가 작업, 브랜치, 구현 결정, 인수인계를 소유한다. Codex와 Claude는 도구이며 소유자가 아니다.
- 각 개발자는 별도 clone 또는 Git worktree를 사용한다. 두 에이전트를 같은 작업 폴더에서 실행하지 않는다.
- 개발 전 `TASK_TEMPLATE.md`로 작업 문서를 만들고 담당자, 리뷰어, 범위, 인수 조건, 의존성, 소유 파일을 기록한다.
- 사용하는 AI와 역할은 무관하다. 구현·리뷰 책임은 작업 문서가 결정한다.

## 2. 작업 흐름
1. **정의**: `docs/tasks/TASK-###-name.md`를 만들고 범위와 인수 조건을 합의한다.
2. **점유**: 변경할 파일과 독점 Unity 에셋을 기록한다.
3. **브랜치**: `feature/TASK-###-name`, `fix/TASK-###-name`, `docs/TASK-###-name`을 사용한다.
4. **구현**: 작업 범위 안에서만 변경하고 의미 있는 단위로 커밋한다.
5. **자체 검증**: 가능한 컴파일·테스트, Unity 확인, `git diff` 검토를 수행한다.
6. **인수인계**: 작업 문서의 Handoff를 작성하고 브랜치를 push한다.
7. **리뷰**: 다른 개발자가 작업 명세와 `REVIEW_CHECKLIST.md`를 기준으로 검토하고 작성자가 수정한다.
8. **수동 테스트**: 사람이 필요한 Unity Scene·Play Mode 동작을 확인한다.
9. **병합**: 인수 조건과 필수 검증이 끝난 뒤 병합한다.

## 3. 병렬 작업
파일과 의존성이 겹치지 않을 때 병렬 작업 가능:
- 서로 분리된 Runtime 시스템
- 안정된 인터페이스에 대한 테스트
- 서로 다른 문서의 기획·설계 작업
- 읽기 전용 조사와 리뷰

다음은 한 명만 소유한다:
- 같은 C# 파일 또는 강하게 결합된 API
- `.unity`, `.prefab`, `.asset`, `.inputactions`
- `ProjectSettings/`, `Packages/manifest.json`, 패키지 lock 파일
- Tag, Layer, Physics, Input, Build 설정
- `.meta`가 바뀌는 에셋 이동·이름 변경

두 작업이 같은 독점 에셋을 필요로 하면 하위 의존 작업을 먼저 병합하고, 다음 담당자가 브랜치를 갱신한 뒤 진행한다.

## 4. Unity 협업 규칙
- 프로젝트는 **Visible Meta Files**와 **Force Text** 직렬화를 사용한다.
- 에셋 이동·이름 변경은 Unity 안에서 수행해 참조를 보존한다.
- 에셋과 해당 `.meta`를 함께 커밋한다.
- Scene/Prefab 변경은 작게 분리하고 무관한 코드 변경과 섞지 않는다.
- 반복 생성·일괄 설정은 Editor Tool을 우선한다.
- Unity MCP 사용 후 변경 파일과 diff를 확인하고 Unity에서 결과를 검증한다.
- `Library/`, `Temp/`, `Logs/`, `obj/`, 개인 `UserSettings/`를 커밋하지 않는다.

## 5. Git 규칙
- 작업 점유 전 fetch 또는 pull하고 공유 의존 파일을 최신화한다.
- 다른 개발자가 사용하는 브랜치에 force push하지 않는다.
- 포맷팅, 리팩터링, 패키지 변경, 기능 구현을 한 작업에 섞지 않는다.
- 커밋 메시지는 의도를 표현한다. 예: `feat(player): add camera-relative movement`.
- Unity 에셋 충돌은 명확한 소유자를 정해 작은 쪽 변경을 Unity에서 다시 만든다. 승인 없이 YAML을 수동 병합하지 않는다.

## 6. 제품·아키텍처 변경
승인된 작업이나 현재 프로토타입 범위를 벗어나는 변경은 다음 순서로 처리한다.
1. 관련 PRD 또는 설계 문서 수정
2. 지속적인 기술 결정이면 ADR 추가·수정
3. 사람 간 합의
4. 구현과 검증

## 7. 최소 인수인계 정보
- 무엇을 왜 변경했는가
- 변경한 파일과 에셋
- 실제 실행한 테스트·검사와 결과
- 남은 Unity 수동 확인
- 알려진 위험·제한·후속 작업
