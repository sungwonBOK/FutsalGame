# Futsal Brawl 에이전트 규칙

## 기본 원칙
- 프로토타입 단계에서는 화려함보다 게임 핵심 재미와 조작 검증에 필요한 최소 구현을 우선한다.
- 새 코드는 읽기 쉽고, 수정 범위가 예측 가능하며, 나중에 확장해도 주변 영향이 적은 구조를 우선한다.
- 폴더 구조는 역할과 책임이 드러나게 유지하고, 새 파일은 찾기 쉽고 응집도 높은 위치에 둔다.
- 공통 규칙이나 구조를 바꿔야 할 때는 먼저 영향 범위를 정리하고 팀이 이해할 수 있는 형태로 남긴다.

## 협업 규칙
- 작업 시작 전 GitHub issue tracker의 진행 상황과 담당 범위를 확인한다.
- 작업 시작 직전에 담당 이슈에 시작 메모를 남기고, 목표와 수정 예정 파일 또는 자산 범위를 기록한다.
- 한 작업에서는 하나의 시스템만 다루고, 그 시스템이 동작하는 데 필요한 최소 변경만 수행한다.
- 다른 활성 작업이 소유한 파일, 자산, 설정은 사전 합의 없이 수정하지 않는다.
- 작업 단위가 끝날 때마다 담당 이슈에 완료 내용, 남은 작업, 확인된 위험이나 blocker를 업데이트한다.
- GitHub에 변경 사항을 올릴 때는 관련 이슈의 진행 상태도 함께 최신으로 맞춘다.

## 구현 규칙
- 기존 패턴을 우선하며 추측성 추상화, 무관한 리팩터링, 요구사항 밖 기능 확장을 하지 않는다.
- 코드는 한 파일에 책임이 과도하게 몰리지 않게 유지하고, 경계가 필요한 경우 작은 단위로 분리한다.
- 새 파일이나 폴더를 어디에 둘지 확신이 없으면 `PROJECT_STRUCTURE.md`를 먼저 확인한다.
- 반복 가능한 수동 작업은 가능하면 재사용 가능한 코드나 도구로 정리한다.
- Runtime과 Editor 코드는 분리하고, Editor 전용 코드는 `Assets/_Game/Scripts/Editor/`에 둔다.
- `.unity`, `.prefab`, `.asset` YAML은 직접 수정하지 않는다. 관련 변경은 Unity Editor, Unity MCP, Editor Tool 같은 안전한 경로를 사용한다.
- `Library/`, `Temp/`, `Logs/`, `obj/`, `UserSettings/`는 수정하거나 커밋하지 않는다.
- `.meta`와 GUID를 보존하고, 에셋 이동이나 이름 변경은 가능하면 Unity에서 수행한다.
- 패키지, 전역 설정, 외부 의존성 추가나 변경은 명시적 승인 없이 진행하지 않는다.

## 변경 전 체크
- `git status`로 작업 트리 상태를 확인한다.
- 대상 코드, 관련 문서, 기존 테스트와 이슈 범위를 확인한다.
- Scene, Prefab, Input Asset, Package, ProjectSettings 변경 전에는 소유권과 충돌 가능성을 다시 확인한다.

## 완료 전 체크
- 가능한 컴파일 검사와 테스트를 실행한다.
- 실제 확인하지 않은 Unity Console 또는 Play Mode 성공을 주장하지 않는다.
- `git diff`를 검토하고 무관하거나 자동 생성된 변경을 제거한다.
- 작업으로 인해 실제 사용 기술, 도구, 구조, 설정이 바뀌었으면 `IMPLEMENTATION_STATUS.md`를 함께 업데이트한다.
- 변경 파일, 동작, 실제 검증 결과, 필요한 수동 확인, 남은 위험을 정리해 공유한다.

협업 절차의 상세 흐름은 `WORKFLOW.md`를 따른다.
